/*
 * 	Author : Rohit Roy
 */

#include "docker.h"
#include <cuda.h>

#define INDEX(x, y, z, size) ((x) + (y) * (size) + (z) * (size) * (size))

void p_generate_surface(SpaceMatrix* space, int thickness) {

}
